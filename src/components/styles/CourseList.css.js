import { style } from '@vanilla-extract/css';

export const list = style({
  marginTop: '4rem',
});

export const heading = style({
  textAlign: 'center',
  fontSize: '2rem',
  fontWeight: 'bold',
  marginBottom: '1rem',
});
